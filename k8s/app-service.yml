kind: Service
apiVersion: v1
metadata:
  name: expense-app-sv
  namespace: expense-app
spec:
  type: NodePort
  selector:
    app: expense-app
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30007
  

